The command is:

make check

When invoked with 'make check', all variables will be properly initialized. 
If you want to run just one specific test, the command is:

make check TESTS=test-adj-generation.sh
